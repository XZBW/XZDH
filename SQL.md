#SQL

-SQL优化面试题

SQL优化相关内容
	1、慢查询
	2、索引
	3、拆分表

SQL优化
	1.尽量使用列名(Oracle9i之后, *和列名一样)
   	  在业务密集的SQL当中尽量不采用IN操作符，用EXISTS 方案代替。
	2.模糊查询like
	  关键词%yue%，由于yue前面用到了“%”，因此该查询必然走全表扫描，除非必要，
	  否则不要在关键词前加%，
	3.尽量使用多表连接（join）查询（避免子查询）
	4.尽量使用where （与having比较）
	  where 先过滤（数据就少了）在分组  